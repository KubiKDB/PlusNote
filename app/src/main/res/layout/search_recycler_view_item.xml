<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/search_container"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginHorizontal="5dp"
        android:layout_marginTop="5dp"
        android:background="@drawable/rectangle_round"
        android:elevation="0dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/search_note_icon"
            android:layout_width="40dp"
            android:layout_height="match_parent"
            android:background="@drawable/ic_textnote"
            app:layout_constraintLeft_toLeftOf="parent" />

        <TextView
            android:id="@+id/search_note_title"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="5dp"
            android:background="@android:color/transparent"
            android:gravity="center_vertical"
            android:paddingBottom="2dp"
            android:textColor="@color/light_blue_text"
            android:singleLine="true"
            android:textSize="21dp"
            app:layout_constraintLeft_toRightOf="@id/search_note_icon"
            app:layout_constraintRight_toLeftOf="@id/search_note_date"/>
        <TextView
            android:id="@+id/search_note_date"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginHorizontal="5dp"
            android:background="@android:color/transparent"
            android:gravity="center_vertical"
            android:paddingBottom="2dp"
            android:textColor="@color/light_blue_text"
            android:singleLine="true"
            android:textSize="21dp"
            app:layout_constraintLeft_toRightOf="@id/search_note_title"
            app:layout_constraintRight_toRightOf="parent"/>
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.constraintlayout.widget.ConstraintLayout>